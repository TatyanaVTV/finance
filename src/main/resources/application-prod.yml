spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/finance_db # имитация доступа по сети
    username: admin
    password: admin
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

security:
  csrf:
    disabled: false

logging:
  level:
    ru.vtvhw.spring.finance: WARN